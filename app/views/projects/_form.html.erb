<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :total_hours_to_finish %>
    <%= f.number_field :total_hours_to_finish %>
  </div>

  <div class="field">
    <%= f.label :worked_hours %>
    <%= f.number_field :worked_hours %>
  </div>

  <div class="field">
    <%= f.label :User_id %>
    <%= f.text_field :User_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
